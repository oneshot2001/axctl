import { program } from './root.js'

// TODO: implement events command
export const eventsCommand = program
  .command('events')
  .description('events — not yet implemented')
  .action(() => {
    console.log('events: coming soon')
  })
