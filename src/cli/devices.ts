import { program } from './root.js'

// TODO: implement devices command
export const devicesCommand = program
  .command('devices')
  .description('devices — not yet implemented')
  .action(() => {
    console.log('devices: coming soon')
  })
