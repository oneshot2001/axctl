import { program } from './root.js'

// TODO: implement discover command
export const discoverCommand = program
  .command('discover')
  .description('discover — not yet implemented')
  .action(() => {
    console.log('discover: coming soon')
  })
